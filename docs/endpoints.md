# Hub endpoints used

| Method | Path | Purpose | Authentication |
|---|---|---|---|
| GET | `/api/agent/targets` | Fetch active targets and protocols | Bearer agent token |
| POST | `/api/agent/samples` | Submit minute latency samples | Bearer agent token |
